[project]
name = "meta-sanctum-tools"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
	"GitPython>=3.1.44",
	"packaging>=24.0",
]

[tool.ruff]
exclude = ["build-homeassistant"]
ignore = ["E402"]
