### Shell environment set up for builds. ###

You can now run 'bitbake <target>'

Common targets are:
    

For homeassistant related images there are images to build from the meta-homeassistant layer:
    core-image-homeassistant
    core-image-homeassistant-full
